# Current Scoring

## 概要

今の長期記憶MVPは、会話文を高度に理解しているわけではありません。  
`text_analyzer.py` の手書きルールで、入力文に対して以下を推定しています。

- どんな種類の記憶か
- どんな感情が含まれていそうか
- 長期記憶として残す価値がどの方向に高いか
- 後でどういう文脈なら思い出してよさそうか
- センシティブな記憶として慎重に扱うべきか

現状は「まずは粗くても記憶を構造化して保存する」ことを優先しています。学習や統計モデルではなく、文字列一致ベースの簡易判定です。

## 入力から保存までの流れ

1. 入力文の前後空白を除去する
2. 正規表現で token らしい文字列を切り出す
3. `topics` と `keywords` を簡易生成する
4. `memory_types` をキーワード一致で推定する
5. `emotion` をキーワード一致で推定する
6. `scores` をルール加点で計算する
7. `recall_policy` をスコアに応じて決める
8. `safety` をセンシティブ語の有無で決める
9. `facets` に用途別の見え方を整理する
10. SQLite に JSON として保存する

## 現在使っている判定材料

今の判定材料は、ほぼ次の定数に限られています。

- `EMOTION_KEYWORDS`
- `MEMORY_TYPE_RULES`
- `FUTURE_TERMS`
- `SENSITIVE_TERMS`
- `TECHNICAL_MARKERS`
- `WORD_RE`

つまり現状は、GiNZA や embedding ではなく「文字列一致 + 手書きスコア加点」で動いています。

## Memory Types

### `preference`

`好き`, `嫌い`, `苦手`, `好む`, `避けたい` が含まれると付きます。  
この入力は、ユーザーの継続的な好みや嫌悪を示している可能性がある、とみなします。

### `desire`

`したい`, `ほしい`, `欲しい`, `望む` が含まれると付きます。  
これは希望や欲求を表す発話として扱います。

### `worry`

`不安`, `心配`, `困る`, `怖い` が含まれると付きます。  
継続的な悩みや気がかりの候補として扱います。

### `reflection`

`思う`, `感じる`, `おかしくないか`, `違和感` が含まれると付きます。  
価値観や自己理解につながる内省として扱います。

### `relationship`

`友達`, `家族`, `母`, `父`, `恋人`, `先輩`, `後輩` が含まれると付きます。  
人間関係に関わる記憶かもしれないとみなします。

### `decision_support`

`決めてほしい`, `選んでほしい`, `どれ`, `迷う` が含まれると付きます。  
ユーザーが選択支援や判断補助を求めている可能性がある、とみなします。

### `task`

`やる`, `作る`, `確認`, `調べる`, `直す` が含まれると付きます。  
タスクやフォローアップ対象として扱います。

### `context`

どのルールにも当たらなかった場合に付くデフォルト値です。  
「意味がない」という意味ではなく、今のルールでは型を付けられなかった、という意味です。

## Emotion

### `joy`

`嬉しい`, `楽しい`, `最高`, `安心`, `よかった` で判定します。  
前向きな感情として扱います。

### `sadness`

`悲しい`, `寂しい`, `つらい`, `しんどい` で判定します。  
落ち込みや喪失感に近い感情として扱います。

### `frustration`

`面倒`, `困る`, `嫌`, `詰まった`, `わからない`, `おかしくないか` で判定します。  
苛立ちや摩擦がある発話として扱います。

### `anxiety`

`不安`, `怖い`, `心配`, `気になる` で判定します。  
気がかりや警戒のある発話として扱います。

### `curiosity`

`気になる`, `知りたい`, `興味`, `試したい` で判定します。  
探索意欲や関心の高さとして扱います。

### `neutral`

上のどれにも当たらなければ `neutral` です。  
感情がないというより、「今の簡易ルールでは強い感情シグナルを見つけられなかった」という意味です。

## Scores

各スコアは 0.0 から 1.0 の範囲に丸められます。  
今の値は学習済みではなく、初期の手書きルールです。

### `persistence_value`

長期記憶として残しておく価値を表します。  
好み、欲求、内省、関係性、感情を含むと上がります。

今の感覚では、「後でまた効きそうな会話か」を粗く見る軸です。

### `retrieval_value`

後で思い出す価値を表します。  
未来表現や選択支援の文脈があると上がります。

今の感覚では、「別のタイミングで会話に再登場しそうか」を見る軸です。

### `affective_value`

感情的な意味の強さを表します。  
感情が `neutral` でないだけで強めに上がります。

今の実装では、感情の粒度よりも「感情を伴う発話かどうか」を重視しています。

### `identity_value`

その人らしさや価値観につながる度合いを表します。  
好み、欲求、内省があると上がります。

今の感覚では、「この人はどういう人かを理解する材料か」を見る軸です。

### `relationship_value`

人間関係に関する重要度を表します。  
関係語が入っていると大きく上がります。

今はかなり単純で、「家族」「恋人」などの語があるかに強く依存しています。

### `practical_value`

日常の判断や支援に使える度合いを表します。  
選択支援、タスク、技術語などがあると上がります。

今の感覚では、「後で役に立つ実務的な記憶か」を見る軸です。

### `task_value`

未完了タスクとして扱うべき度合いを表します。  
タスク語や未来語があると上がります。

フォローアップ対象かどうかを粗く見るための値です。

### `decision_value`

選択や判断の材料として重要かを表します。  
今は主に `decision_support` に反応して上がります。

「どれを選ぶか」「どう決めるか」の文脈に寄ったスコアです。

### `indexability_value`

検索や分類のしやすさを表します。  
topic 数が多い、技術語がある、という条件で上がります。

今の感覚では、「後で探す手がかりが多い発話か」を見る軸です。

## Recall Policy

`recall_policy` は、記憶をどう出し直すかの簡易ルールです。

### `mode`

- `normal`
  通常の記憶として扱う
- `gentle`
  感情価値が高いので、やや丁寧に持ち出す
- `explicit_only`
  センシティブ語が含まれるので、明示的な文脈でのみ扱う

### `allowed_contexts`

思い出してよい文脈です。  
今は `related_topic` を基本に、`decision_support`, `follow_up`, `emotional_support` を追加します。

### `avoid_contexts`

不用意に思い出さない方がよい文脈です。  
感情価値が高いと `unrelated_smalltalk`、センシティブなら `casual_joking` も避けます。

### `suggested_phrasing`

今は単純に、元の文の先頭40文字を使って「以前、〜という話があったけど」と返すだけです。  
まだ自然な想起文生成ではありません。

### `auto_recall_threshold`

自動想起の強さの目安です。  
`gentle` のときは高め、通常時はやや低めです。

## Safety

### `sensitivity`

`病気`, `トラウマ`, `家族`, `お金`, `秘密`, `恋人` があると `sensitive`、なければ `normal` です。  
これは保存可否ではなく、扱いの慎重さを示します。

### `privacy_level`

今はすべて `personal` 固定です。  
将来的に `public`, `private` などへ分ける余地があります。

### `stability`

今はすべて `medium` 固定です。  
将来的には「一時的な気分」か「長期的な価値観」かで分けたいです。

### `source_confidence`

今は `0.9` 固定です。  
本人の直接発話をそのまま保存する前提だからです。

### `needs_confirmation`

今は `False` 固定です。  
推測保存や曖昧抽出を後で導入したら意味が出ます。

## 限界

今のスコアリングには、はっきりした限界があります。

- GiNZA を本体でまだ使っていない
- 否定や文脈の取り違えに弱い
- `reason_codes` がなく、なぜ加点されたかを結果へ残していない
- `should_save` をまだ明示的に返していない
- 実データによる評価・調整ループがない

つまり、今は「保存ルールの初期たたき台」であって、精度の高い記憶判定器ではありません。

## 次に更新すべき項目

今後スコアを改善するなら、少なくとも次をこの文書へ追記すべきです。

- `should_save`
- `save_score`
- `reason_codes`
- スコア変更の理由
- 誤判定の具体例
